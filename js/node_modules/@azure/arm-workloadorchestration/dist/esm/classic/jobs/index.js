// Copyright (c) Microsoft Corporation.
// Licensed under the MIT License.
import { listByTarget, get } from "../../api/jobs/operations.js";
function _getJobs(context) {
    return {
        listByTarget: (resourceUri, options) => listByTarget(context, resourceUri, options),
        get: (resourceUri, jobName, options) => get(context, resourceUri, jobName, options),
    };
}
export function _getJobsOperations(context) {
    return {
        ..._getJobs(context),
    };
}
//# sourceMappingURL=index.js.map