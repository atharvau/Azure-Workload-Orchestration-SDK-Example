import { AzureSupportedClouds } from "../static-helpers/cloudSettingHelpers.js";
import { Client, ClientOptions } from "@azure-rest/core-client";
import { TokenCredential } from "@azure/core-auth";
/** Microsoft.Edge Resource Provider management API. */
export interface WorkloadOrchestrationManagementContext extends Client {
    /** The API version to use for this operation. */
    /** Known values of {@link KnownVersions} that the service accepts. */
    apiVersion: string;
    /** The ID of the target subscription. The value must be an UUID. */
    subscriptionId: string;
}
/** Optional parameters for the client. */
export interface WorkloadOrchestrationManagementClientOptionalParams extends ClientOptions {
    /** The API version to use for this operation. */
    /** Known values of {@link KnownVersions} that the service accepts. */
    apiVersion?: string;
    /** Specifies the Azure cloud environment for the client. */
    cloudSetting?: AzureSupportedClouds;
}
/** Microsoft.Edge Resource Provider management API. */
export declare function createWorkloadOrchestrationManagement(credential: TokenCredential, subscriptionId: string, options?: WorkloadOrchestrationManagementClientOptionalParams): WorkloadOrchestrationManagementContext;
//# sourceMappingURL=workloadOrchestrationManagementContext.d.ts.map